# THE FINDGROUP theme folder.
# Kept minimal on purpose — some Apache/cPanel configs disallow Options and
# Require directives in subdirectory .htaccess files, which causes 500 errors.
# Directory-listing off is the only safe directive here.
Options -Indexes
